- Keep exactly five root workspace crates with these dependency edges only: `pi-agent -> pi-ai`, `pi-ext -> {pi-ai, pi-agent, pi-tui}`, and `pi -> {pi-ai, pi-agent, pi-ext, pi-tui}`; `pi-ai` and `pi-tui` must have no workspace dependencies. This topology preserves the provider, agent, extension, terminal, and product ownership boundaries.
- Add a dependency only with its first callsite, after rechecking the current crate release and upstream repository for maintenance, yank status, Rust 1.97.1 compatibility, and an allowed license. This prevents speculative edges and stale or unusable pins from entering the lockfile.
- Keep `pi-tui` product-agnostic and free of workspace dependencies. Terminal components must remain reusable without provider, agent, extension, or product policy.
- Define serializable UI values in `pi-tui` during Phase 4. This keeps the native UI contract owned by the crate that interprets and renders it.
- Define the JSONL extension envelope once in `pi-ext` during Phase 6, and implement the TypeScript side as a portable protocol client rather than a second authority. This prevents the Rust and TypeScript wire contracts from drifting.
- Keep Bun out of built-in provider requests and native paint. Bun is permitted only in the bundled TypeScript extension host and extension-defined custom providers so native operation does not inherit host latency or availability.
- Preserve wire and behavior compatibility with `.references/pi/packages/{ai,agent,coding-agent,tui}` rather than object-order or implementation identity. Those four packages are the authority for serialized names and observable contracts.
- Omit only the settled exclusions: experimental `orchestrator`, the duplicate unused `AgentHarness` surface, `armin.ts`, `daxnuts.ts`, `earendil-announcement.ts`, announcement animations and network fetches, decorative easter eggs, install telemetry, and modifyOtherKeys-only modified Enter on legacy non-Kitty terminals. Treat every other compatibility surface as required so omissions cannot expand by convenience.
