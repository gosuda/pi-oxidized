/**
 * Compatibility version of the reference `@earendil-works/pi-coding-agent`
 * package this host is built against. The hello handshake rejects any peer
 * advertising a different version before any registration runs.
 */
export const COMPATIBILITY_VERSION = "0.80.10";
